# Dawn of Stellar - 기믹 UI 디스플레이 설계 문서

> **작성일**: 2025-11-17
> **버전**: 5.1.0
> **목적**: 재설계된 직업 기믹을 시각적으로 표시하는 UI 시스템 설계

---

## 📋 목차

1. [UI 설계 원칙](#ui-설계-원칙)
2. [공통 UI 구조](#공통-ui-구조)
3. [직업별 UI 설계](#직업별-ui-설계)
4. [구현 방안](#구현-방안)

---

## 🎯 UI 설계 원칙

### 1. 명확성 (Clarity)
- 기믹 상태를 한눈에 파악 가능
- 숫자와 게이지를 함께 표시
- 색상 코딩으로 구간 표시

### 2. 일관성 (Consistency)
- 모든 직업이 같은 레이아웃 구조
- 유사한 메커니즘은 유사한 UI
- 표준 색상 팔레트 사용

### 3. 정보 밀도 (Information Density)
- 필수 정보만 표시
- 상세 정보는 토글 가능
- 전투 중에는 간략한 형태

### 4. 반응성 (Responsiveness)
- 기믹 값 변화 시 즉시 업데이트
- 애니메이션으로 변화 표시
- 경고 알림 (오버히트, 탄창 비움 등)

---

## 📐 공통 UI 구조

모든 직업의 기믹 UI는 다음 공통 구조를 따릅니다:

```
┌─────────────────────────────────────────┐
│  [직업 아이콘] 직업명 - 기믹명          │
├─────────────────────────────────────────┤
│  [게이지/표시기]                         │
│  - 현재 값 / 최대값                      │
│  - 구간 표시 (색상)                      │
│  - 특수 효과 아이콘                      │
├─────────────────────────────────────────┤
│  📊 현재 상태:                           │
│  - 활성 효과 목록                        │
│  - 보너스/패널티                         │
│  - 다음 행동 추천 (선택 사항)             │
└─────────────────────────────────────────┘
```

### 색상 팔레트

| 색상 | 용도 | 예시 |
|------|------|------|
| 🔵 파란색 | 안전/냉각 구간 | 기계공학자 냉각 구간 |
| 🟢 초록색 | 최적 구간 | 기계공학자 최적 구간, 시간술사 현재 |
| 🟡 노란색 | 주의 구간 | 기계공학자 위험 구간 |
| 🔴 빨간색 | 위험 구간 | 오버히트, 탄창 비움 |
| 🟣 보라색 | 특수 상태 | 버프 활성화 |
| ⚪ 흰색 | 중립 | 기본 상태 |

---

## 🎮 직업별 UI 설계

---

## 1. ⚙️ 기계공학자 (Engineer) - 열 관리 시스템

### UI 레이아웃

```
┌──────────────────────────────────────────┐
│  ⚙️ 기계공학자 - 열 관리                 │
├──────────────────────────────────────────┤
│  열 게이지:                               │
│  [🔵🔵🔵🟢🟢🟢🟢🟡🟡🔴] 75/100          │
│   냉각   최적   위험  오버히트             │
│   0----50----79----99--100               │
│           ↑ 현재                         │
├──────────────────────────────────────────┤
│  🔥 상태: 최적 구간                       │
│  ⚡ 효과:                                 │
│    • 공격력 +30%                         │
│    • 스킬 효과 +20%                      │
│    • 기계 효율 발동                       │
│  📊 다음 턴 자동 열 증가: +5              │
│                                          │
│  ⚠️ 경고: 위험 구간 진입 임박 (95 도달 시 자동 냉각) │
└──────────────────────────────────────────┘
```

### 구간별 표시

| 구간 | 색상 | 아이콘 | 메시지 |
|------|------|--------|--------|
| 냉각 (0-49) | 🔵 파란색 | ❄️ | "정상 - 약한 화력" |
| 최적 (50-79) | 🟢 초록색 | ⚡ | "최적 - 강력한 화력" |
| 위험 (80-99) | 🟡 노란색 | 🔥 | "위험 - 매우 강력하지만 리스크" |
| 오버히트 (100) | 🔴 빨간색 | 💥 | "오버히트! 2턴 스턴" |

### 애니메이션

- 열 증가: 게이지가 오른쪽으로 채워짐
- 열 감소: 게이지가 왼쪽으로 비워짐
- 구간 전환: 색상 변화 + 깜빡임
- 오버히트: 화면 전체 붉은 깜빡임 + 경고음

---

## 2. 🎯 저격수 (Sniper) - 탄창 재장전 시스템

### UI 레이아웃

```
┌──────────────────────────────────────────┐
│  🎯 저격수 - 탄창 관리                    │
├──────────────────────────────────────────┤
│  탄창: 6/6                                │
│  [■][■][P][E][■][■]                     │
│   1  2  3  4  5  6                        │
│              ↑ 다음 발사                  │
│                    ↑ 라스트 불렛 보너스   │
├──────────────────────────────────────────┤
│  📦 탄환 범례:                            │
│   ■ = 기본 탄환 (배율 2.0)                │
│   P = 관통탄 (방어 무시 50%, 배율 2.5)     │
│   E = 폭발탄 (광역 50%, 배율 1.8)          │
│   F = 빙결탄 (빙결, 배율 1.8)              │
│   🔥 = 화염탄 (화상 3턴, 배율 2.0)         │
│   ☠️ = 독침탄 (독 5턴, 배율 1.5)           │
│   ✨ = 섬광탄 (명중률 -50% 2턴)            │
│   💀 = 헤드샷 탄 (크리티컬 100%, 배율 5.0) │
│                                          │
│  🌟 다음 탄환: 관통탄                     │
│     - 방어 무시 50%                       │
│     - 배율 2.5                            │
│                                          │
│  💡 라스트 불렛 (6번): 크리티컬 +50%, 데미지 +30% │
└──────────────────────────────────────────┘
```

### 탄창 비움 경고

```
┌──────────────────────────────────────────┐
│  🎯 저격수 - 탄창 관리                    │
├──────────────────────────────────────────┤
│  탄창: 0/6                                │
│  [□][□][□][□][□][□]                     │
│                                          │
│  ⚠️⚠️⚠️ 탄창 비음! ⚠️⚠️⚠️              │
│  🔫 권총 모드 (배율 0.8)                  │
│  💡 즉시 재장전 필요!                     │
└──────────────────────────────────────────┘
```

### 특수 탄환 장전 시 애니메이션

```
턴 1:
탄창: [■][■][□][□][□][□]
     ↓ 관통탄 2발 장전 ↓
턴 2:
탄창: [■][■][P][P][□][□]
        (깜빡임 효과)
```

---

## 3. ⏰ 시간술사 (Time Mage) - 타임라인 균형 시스템

### UI 레이아웃

```
┌──────────────────────────────────────────┐
│  ⏰ 시간술사 - 타임라인                   │
├──────────────────────────────────────────┤
│  타임라인:                                │
│  [과거]      [현재]      [미래]           │
│  -5──-3──-1──[0]──+1──+3──+5             │
│          ↑ 현재 위치                      │
│                                          │
│  색상 표시:                               │
│  [🔵🔵🔵🟢🟢🟢🟢🟡🟡🟡🟡]              │
│   먼과거 과거 약간과거 현재 약간미래 미래 먼미래 │
├──────────────────────────────────────────┤
│  📍 상태: 현재 (균형)                     │
│  ⚡ 효과:                                 │
│    • 모든 스킬 효과 +40%                  │
│    • 속도 +30%                           │
│    • MP 회복 2배                         │
│    • 크리티컬 +20%                       │
│                                          │
│  💡 사용 가능 스킬:                       │
│    • 시간의 균형 (현재 전용, 배율 4.5)     │
│    • 찰나 (추가 행동)                     │
└──────────────────────────────────────────┘
```

### 타임라인별 표시

| 타임라인 | 색상 | 아이콘 | 상태 메시지 |
|----------|------|--------|------------|
| -5 | 🔵🔵 | ⏪⏪ | "먼 과거 - 모든 효과 -30%, 속도 -50%" |
| -3~-4 | 🔵 | ⏪ | "과거 - 과거 스킬 효과 +20%, 속도 -20%" |
| -1~-2 | 🔵 | ⏪ | "약간 과거 - 과거 스킬 효과 +10%" |
| 0 | 🟢 | 🌟 | "현재 - 모든 효과 +40% (최적!)" |
| +1~+2 | 🟡 | ⏩ | "약간 미래 - 미래 스킬 효과 +10%" |
| +3~+4 | 🟡 | ⏩ | "미래 - 미래 스킬 효과 +20%, 선제공격 +30%" |
| +5 | 🔴 | ⏩⏩ | "먼 미래 - 모든 효과 -30%, 혼란" |

### 스킬 사용 시 타임라인 이동 표시

```
턴 1: 타임라인 0
     ↓ 슬로우 사용 (-1 이동)
턴 2: 타임라인 -1
     ↓ 헤이스트 사용 (+1 이동)
턴 3: 타임라인 0 (균형 복귀!)
```

---

## 4. 🥋 몽크 (Monk) - 음양 기 흐름 시스템

### UI 레이아웃

```
┌──────────────────────────────────────────┐
│  🥋 몽크 - 음양 기 흐름                   │
├──────────────────────────────────────────┤
│  기 게이지:                               │
│  [陰]           [☯]           [陽]       │
│  0───25───50───75───100                  │
│           ↑ 현재 (균형)                   │
│                                          │
│  🌟 상태: 태극 조화 (균형)                │
│  ⚡ 효과:                                 │
│    • 모든 스탯 +20%                       │
│    • 음양 스킬 강화 +30%                  │
│    • HP/MP 회복 매 턴 5%                  │
│                                          │
│  🥊 콤보: 5 연속                          │
│    • 데미지 +40%                         │
│    • 크리티컬 +30%                       │
│    • 무쌍 오의까지 2 콤보 남음!            │
└──────────────────────────────────────────┘
```

### 기운별 오라 효과

| 기운 구간 | 색상 | 오라 | 효과 표시 |
|-----------|------|------|----------|
| 음 (0-25) | 🔵 파란색 | 파란 오라 | "방어력 +50%, MP 회복 +100%" |
| 조화 (40-60) | 🟡 금색 | 금색 오라 | "모든 스탯 +20% (최적!)" |
| 양 (75-100) | 🔴 빨간색 | 빨간 오라 | "공격력 +40%, 속도 +30%" |

### 콤보 표시

```
🥊 콤보 게이지:
[■][■][■][■][■][□][□][□][□][□]  5/10

콤보 3: 데미지 +20% ✓
콤보 5: 데미지 +40%, 크리티컬 +30% ✓
콤보 7: 데미지 +60%, 무쌍 오의 해금 (다음 목표!)
```

---

## 5. ⚔️🔮 배틀메이지 (Battle Mage) - 룬 공명 시스템

### UI 레이아웃

```
┌──────────────────────────────────────────┐
│  ⚔️🔮 배틀메이지 - 룬 공명                │
├──────────────────────────────────────────┤
│  현재 룬:                                 │
│  [🔥][🔥][❄️][⚡][🔥]                    │
│   1   2   3   4   5                      │
│                                          │
│  📊 룬 분석:                              │
│    🔥 화염 룬: 3개                        │
│    ❄️ 빙결 룬: 1개                        │
│    ⚡ 번개 룬: 1개                        │
├──────────────────────────────────────────┤
│  🔍 감지된 공명 패턴:                     │
│    ✨ 화염 룬 3개 → 화염 폭발!            │
│                                          │
│  💡 추천 행동:                            │
│    "룬 폭발" 사용 시 화염 폭발 발동       │
│    효과: 광역 화상 (3턴, 매 턴 10% HP)    │
│                                          │
│  📋 공명 패턴 목록:                       │
│    🔥🔥🔥 = 화염 폭발                     │
│    ❄️❄️❄️ = 빙하기                       │
│    ⚡⚡⚡ = 연쇄 번개                      │
│    🔥❄️ = 증기 폭발                       │
│    🔥❄️⚡ = 원소 융합                     │
└──────────────────────────────────────────┘
```

### 룬 공명 애니메이션

```
룬 폭발 시전!
[🔥][🔥][🔥][❄️][⚡]
  ↓ ↓ ↓ (빛나는 효과)
화염 폭발 발동!
(화염 룬 3개가 폭발 애니메이션)
```

---

## 6. 💀 네크로맨서 (Necromancer) - 언데드 군단 관리

### UI 레이아웃

```
┌──────────────────────────────────────────┐
│  💀 네크로맨서 - 언데드 군단              │
├──────────────────────────────────────────┤
│  시체: 7개 / 10개                         │
│  [💀][💀][💀][💀][💀][💀][💀]          │
│                                          │
│  소환된 언데드: (3/3)                     │
│  ┌────────────────────────────┐         │
│  │ [⚔️ 스켈레톤]                │         │
│  │  HP: 80/100                 │         │
│  │  역할: 공격                  │         │
│  │  상태: 정상                  │         │
│  └────────────────────────────┘         │
│  ┌────────────────────────────┐         │
│  │ [🛡️ 좀비]                   │         │
│  │  HP: 120/150                │         │
│  │  역할: 탱커                  │         │
│  │  상태: 정상                  │         │
│  └────────────────────────────┘         │
│  ┌────────────────────────────┐         │
│  │ [👻 유령]                    │         │
│  │  HP: 40/60                  │         │
│  │  역할: 디버프                │         │
│  │  상태: 정상                  │         │
│  └────────────────────────────┘         │
│                                          │
│  💰 유지 비용: 15 MP/턴                   │
└──────────────────────────────────────────┘
```

### 언데드 소환 가능 표시

```
📋 소환 가능:
✓ 스켈레톤 (시체 1개) - 소환 가능
✓ 좀비 (시체 2개) - 소환 가능
✓ 유령 (시체 1개) - 소환 가능
✓ 뱀파이어 (시체 3개) - 소환 가능
✓ 리치 (시체 3개) - 소환 가능
✓ 데스나이트 (시체 5개) - 소환 가능
✗ 데스 드래곤 (시체 10개) - 시체 부족!
```

---

## 7~15. 나머지 직업 UI 설계 (간략)

### 7. 😡 광전사 (Berserker) - 광기 역치 시스템

```
[정상]    [광전사]    [위험]    [폭주!]
0────30────70────100
         ↑ 광기: 65
상태: 광전사 모드 (공격력 +60%)
```

### 8. 🧛 흡혈귀 (Vampire) - 갈증 관리 시스템

```
[만족]    [갈증]    [극심한 갈증]    [혈액 광란]
0────30────60────90────100
              ↑ 갈증: 70
상태: 극심한 갈증 (공격력 +70%, 흡혈 3배)
```

### 9. 💻 해커 (Hacker) - 멀티스레드 시스템

```
실행 중인 프로그램: (3/3)
[💾 바이러스] - 적 공격력 -20%
[🔓 백도어] - 적 방어력 -30%
[⚡ DDoS] - 적 속도 -50%
CPU 사용률: 100%
유지 비용: 15 MP/턴
```

### 10. ⚔️ 검투사 (Gladiator) - 군중의 환호 시스템

```
환호 게이지: 85/100
[████████░░] ⭐⭐⭐⭐
상태: 열광 (환호 80+)
효과: 공격력 +60%, 크리티컬 +40%, 모든 공격 광역화
다음 단계: 검투사의 영광 (100) - 무적 3턴!
```

### 11. 🗡️ 암살자 (Assassin) - 은신-노출 딜레마

```
상태: 🌑 은신
효과:
  • 회피율 +80%
  • 다음 공격 크리티컬 확정
패널티:
  • 이동 속도 -50%
  • 공격 시 은신 해제
은신 남은 턴: 무제한 (공격 전까지)
```

### 12. 🏹 궁수 (Archer) - 지원사격 시스템

```
마킹된 아군: (3/3)

[전사] 🎯
 화살: 관통 화살 (방어 무시)
 남은 지원: 3회

[검성] 🎯
 화살: 화염 화살 (화상)
 남은 지원: 2회

[광전사] 🎯
 화살: 폭발 화살 (광역)
 남은 지원: 3회

🔥 콤보: 5 연속
  - 데미지 +60%
  - 크리티컬 +40%

💡 다음 지원 2회면 완벽한 지원!
```

### 13. 🌊 정령술사 (Elementalist) - 4대 정령 소환

```
소환된 정령: (2/2)

[🔥 화염 정령]
 HP: 50/50
 능력: 화염 공격

[💧 물 정령]
 HP: 60/60
 능력: 회복

🌀 융합 스킬 해금:
  • 화염 + 물 = 증기 폭발
```

### 14. 📚 철학자 (Philosopher) - 딜레마 선택 시스템

```
현재 딜레마:
"힘 vs 지혜"

선택지:
[A] 힘 → 공격력 +50%
[B] 지혜 → 마법 +50%

선택 기록:
힘: ■■■■■ (5회)
지혜: ■■ (2회)

⚠️ 힘 선택 5회 달성!
특수 효과 "전사의 길" 발동!
```

### 15. 🌀 차원술사 (Dimensionist) - 확률 왜곡 게이지

```
확률 왜곡 게이지: 75/100
[███████░░░]

사용 가능:
✓ 크리티컬 확률 +50% (20 소모)
✓ 회피율 +80% (30 소모)
✓ 실패 재시도 (50 소모)
✗ 평행우주 (100 소모) - 게이지 부족!

게이지 획득: 매 턴 +10, 크리티컬 시 +20
```

---

## 🛠️ 구현 방안

### 1. UI 컴포넌트 구조

```python
# src/ui/gimmick_display.py

class GimmickDisplay:
    """기믹 UI 디스플레이 베이스 클래스"""

    def __init__(self, character):
        self.character = character
        self.gimmick_type = character.gimmick_type

    def render(self) -> str:
        """기믹 UI를 렌더링하여 문자열로 반환"""
        raise NotImplementedError

    def get_status_color(self) -> str:
        """현재 상태에 따른 색상 반환"""
        raise NotImplementedError


class HeatGaugeDisplay(GimmickDisplay):
    """열 게이지 디스플레이 (기계공학자)"""

    def render(self) -> str:
        heat = self.character.heat
        max_heat = self.character.max_heat

        # 구간 판정
        if heat < 50:
            zone = "냉각 구간"
            color = "blue"
        elif heat < 80:
            zone = "최적 구간"
            color = "green"
        elif heat < 100:
            zone = "위험 구간"
            color = "yellow"
        else:
            zone = "오버히트!"
            color = "red"

        # 게이지 그리기
        gauge = self._draw_gauge(heat, max_heat, color)

        return f"""
┌──────────────────────────────────────────┐
│  ⚙️ 기계공학자 - 열 관리                 │
├──────────────────────────────────────────┤
│  열 게이지:                               │
│  {gauge} {heat}/{max_heat}              │
│                                          │
│  🔥 상태: {zone}                         │
│  {self._get_zone_effects(heat)}          │
└──────────────────────────────────────────┘
        """

    def _draw_gauge(self, current, maximum, color):
        """게이지 바 그리기"""
        filled = int((current / maximum) * 40)
        bar = "█" * filled + "░" * (40 - filled)
        return f"[{bar}]"

    def _get_zone_effects(self, heat):
        """현재 구간의 효과 텍스트 반환"""
        if heat < 50:
            return "⚡ 효과: 일반 공격력"
        elif heat < 80:
            return "⚡ 효과: 공격력 +30%, 스킬 효과 +20%"
        elif heat < 100:
            return "⚡ 효과: 공격력 +50%, 크리티컬 +15% (받는 피해 +20%)"
        else:
            return "💥 스턴 2턴! 열 리셋!"


class MagazineDisplay(GimmickDisplay):
    """탄창 디스플레이 (저격수)"""

    BULLET_ICONS = {
        "normal": "■",
        "penetrating": "P",
        "explosive": "E",
        "frost": "F",
        "fire": "🔥",
        "poison": "☠️",
        "flash": "✨",
        "headshot": "💀"
    }

    def render(self) -> str:
        magazine = self.character.magazine
        max_mag = self.character.max_magazine

        # 탄창 표시
        bullets = []
        for i in range(max_mag):
            if i < len(magazine):
                bullet_type = magazine[i]
                bullets.append(self.BULLET_ICONS.get(bullet_type, "■"))
            else:
                bullets.append("□")

        bullet_str = "][".join(bullets)

        # 다음 발사할 탄환
        if len(magazine) > 0:
            next_bullet = magazine[0]
            next_info = self.character.bullet_types.get(next_bullet, {})
        else:
            next_bullet = None
            next_info = {}

        return f"""
┌──────────────────────────────────────────┐
│  🎯 저격수 - 탄창 관리                    │
├──────────────────────────────────────────┤
│  탄창: {len(magazine)}/{max_mag}         │
│  [{bullet_str}]                          │
│                                          │
│  {self._get_magazine_status(magazine)}   │
└──────────────────────────────────────────┘
        """

    def _get_magazine_status(self, magazine):
        """탄창 상태 메시지"""
        if len(magazine) == 0:
            return "⚠️⚠️⚠️ 탄창 비음! 즉시 재장전 필요! ⚠️⚠️⚠️"
        elif len(magazine) == 1:
            return "🌟 마지막 탄환 (라스트 불렛 보너스!)"
        else:
            return f"🌟 다음 탄환: {self.character.bullet_types.get(magazine[0], {}).get('name', '기본')}"


class TimelineDisplay(GimmickDisplay):
    """타임라인 디스플레이 (시간술사)"""

    def render(self) -> str:
        timeline = self.character.timeline

        # 타임라인 바 그리기
        timeline_bar = self._draw_timeline(timeline)

        # 현재 상태 판정
        status = self._get_timeline_status(timeline)

        return f"""
┌──────────────────────────────────────────┐
│  ⏰ 시간술사 - 타임라인                   │
├──────────────────────────────────────────┤
│  {timeline_bar}                          │
│  -5──-3──-1──[0]──+1──+3──+5             │
│          ↑ {timeline:+d}                 │
│                                          │
│  📍 {status}                             │
└──────────────────────────────────────────┘
        """

    def _draw_timeline(self, timeline):
        """타임라인 바 그리기"""
        # -5 ~ +5를 0 ~ 10으로 매핑
        pos = timeline + 5
        bar = ["░"] * 11
        bar[pos] = "■"
        return "[" + "".join(bar) + "]"

    def _get_timeline_status(self, timeline):
        """타임라인 상태 메시지"""
        if timeline == 0:
            return "상태: 현재 (균형) - 모든 효과 +40%!"
        elif timeline <= -3:
            return "상태: 먼 과거 - 모든 효과 -30%"
        elif timeline < 0:
            return "상태: 과거 - 과거 스킬 강화"
        elif timeline >= 3:
            return "상태: 먼 미래 - 선제공격 확률 높음"
        else:
            return "상태: 미래 - 미래 스킬 강화"
```

### 2. UI 매니저

```python
# src/ui/gimmick_ui_manager.py

class GimmickUIManager:
    """기믹 UI 관리자"""

    DISPLAY_MAP = {
        "heat_management": HeatGaugeDisplay,
        "magazine_system": MagazineDisplay,
        "timeline_system": TimelineDisplay,
        "yin_yang_flow": YinYangDisplay,
        "rune_resonance": RuneResonanceDisplay,
        # ... 나머지 직업
    }

    def __init__(self):
        self.displays = {}

    def register_character(self, character):
        """캐릭터의 기믹 UI 등록"""
        gimmick_type = character.gimmick_type
        display_class = self.DISPLAY_MAP.get(gimmick_type)

        if display_class:
            self.displays[character.name] = display_class(character)

    def render(self, character_name: str) -> str:
        """캐릭터의 기믹 UI 렌더링"""
        display = self.displays.get(character_name)
        if display:
            return display.render()
        return ""

    def render_all(self) -> str:
        """모든 캐릭터의 기믹 UI 렌더링"""
        result = []
        for name, display in self.displays.items():
            result.append(display.render())
        return "\n\n".join(result)
```

### 3. 전투 화면 통합

```python
# src/ui/combat_ui.py

class CombatUI:
    """전투 UI 통합 화면"""

    def __init__(self, combat_manager):
        self.combat_manager = combat_manager
        self.gimmick_ui = GimmickUIManager()

        # 모든 파티원 등록
        for char in combat_manager.party:
            self.gimmick_ui.register_character(char)

    def render_full_screen(self):
        """전체 전투 화면 렌더링"""
        # 상단: 전투 상태
        # 중단: 캐릭터 스탯 + 기믹 UI
        # 하단: 행동 메뉴

        screen = []
        screen.append(self._render_combat_status())
        screen.append(self._render_party_status())
        screen.append(self._render_gimmick_displays())
        screen.append(self._render_action_menu())

        return "\n".join(screen)

    def _render_gimmick_displays(self):
        """기믹 UI 표시"""
        # 현재 턴 캐릭터의 기믹만 표시 (또는 전체)
        current_char = self.combat_manager.current_character
        return self.gimmick_ui.render(current_char.name)
```

### 4. 색상 지원 (옵션)

```python
# src/ui/colors.py

class Colors:
    """ANSI 색상 코드"""
    RESET = "\033[0m"
    BLUE = "\033[94m"
    GREEN = "\033[92m"
    YELLOW = "\033[93m"
    RED = "\033[91m"
    PURPLE = "\033[95m"

    @staticmethod
    def colorize(text, color):
        """텍스트에 색상 적용"""
        return f"{color}{text}{Colors.RESET}"

# 사용 예시:
print(Colors.colorize("냉각 구간", Colors.BLUE))
print(Colors.colorize("최적 구간", Colors.GREEN))
print(Colors.colorize("위험 구간", Colors.YELLOW))
print(Colors.colorize("오버히트!", Colors.RED))
```

---

## 📊 우선순위 구현 순서

### Phase 1: 핵심 3개 (2주)
1. ⚙️ 기계공학자 - 열 게이지
2. 🎯 저격수 - 탄창 표시
3. ⏰ 시간술사 - 타임라인

### Phase 2: 추가 6개 (1개월)
4. 🥋 몽크 - 음양 게이지
5. ⚔️🔮 배틀메이지 - 룬 표시
6. 💀 네크로맨서 - 언데드 목록
7. 😡 광전사 - 광기 게이지
8. 🧛 흡혈귀 - 갈증 게이지
9. 💻 해커 - 프로그램 목록

### Phase 3: 나머지 (2개월)
10~15. 검투사, 암살자, 궁수, 정령술사, 철학자, 차원술사

---

## ✅ 체크리스트

- [ ] 각 직업별 Display 클래스 구현
- [ ] GimmickUIManager 구현
- [ ] CombatUI 통합
- [ ] 색상 지원 추가
- [ ] 애니메이션 효과 구현
- [ ] 모바일/작은 화면 지원
- [ ] 테스트 코드 작성
- [ ] 문서화 완료

---

**작성자**: Claude Code
**다음 단계**: Phase 1 UI 구현 후 피드백 수집 및 개선
