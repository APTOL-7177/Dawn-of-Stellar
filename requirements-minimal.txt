# Dawn of Stellar - 최소 의존성 (게임 실행에 필요한 핵심 패키지만)

# 필수 패키지
pyyaml>=6.0           # YAML 설정 파일 로드
tcod>=16.0.0          # 로그라이크 게임 엔진 (렌더링, 입력, FOV 등)
numpy>=1.26.0         # tcod 의존성 (배열 처리)

# 선택 패키지 (없어도 게임 실행 가능)
# pytest>=7.4.0       # 테스트 실행용
# pylint>=3.0.0       # 코드 품질 검사용
